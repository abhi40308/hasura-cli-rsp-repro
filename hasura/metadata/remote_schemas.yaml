- name: test
  definition:
    url: https://1fyli.sse.codesandbox.io/
    timeout_seconds: 60
  permissions:
  - role: abcd
    definition:
      schema: |-
        schema  { query: Query }
        type Query { launches: [Launch]! }
        type Launch { id: ID! site: String mission: Mission rocket: Rocket isBooked: Boolean! }
        type Mission { name: String missionPatch: String }
        type Rocket { id: ID! name: String type: String }
  - role: test
    definition:
      schema: |-
        schema  { query: Query }
        type Query { launches: [Launch]! }
        type Launch { id: ID! site: String mission: Mission rocket: Rocket isBooked: Boolean! }
        type Mission { name: String missionPatch: String }
        type Rocket { id: ID! name: String type: String }
